using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Auto_destruction : MonoBehaviour
{
    // Start is called before the first frame update
    public void autodestruction() 
    {
        Destroy(this.gameObject);
    
    }
}
